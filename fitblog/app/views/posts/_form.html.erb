<script>
  $(function() {
    $( "#datepicker" ).datepicker();
  });
</script>

<%= form_for @post do |f| %>
	<% if @post.errors.any? %>
		<%= pluralize(@post.errors.count, "error") %> prohibited the post from being saved:
		<ul>
		<% @post.errors.each do |error| %>
			<li><%= error %></li>
		<% end %>
		</ul>
	<% end %>
	<p><%= f.label :title %><br><%= f.text_field :title, class: '' %><br>
	<%= f.label :post_date %><br><%= f.date_select :post_date %><br>
	<%= f.label :body %><br><%= f.text_area :body %></p>
	<%= f.submit %>
<% end %>